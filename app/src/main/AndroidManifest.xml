<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tech.hans.glucotracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".readArticles"></activity>
        <activity android:name=".meditateView" />
        <activity android:name=".generateReport" />
        <activity android:name=".viewGraph" />
        <activity android:name=".viewReading" />
        <activity android:name=".enterReading" />
        <activity android:name=".Resulter" />
        <activity android:name=".SelfAssessment" />
        <activity android:name=".RecoverPassword" />
        <activity android:name=".HomePage" />
        <activity android:name=".LogInUser" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>